version: '3'

services:
  subscriber:
    build: subscriber
    ports:
      - "3306:3306"
    expose:
      - "3306"
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_USER: "user"
      MYSQL_PASSWORD: "password"
      MYSQL_DATABASE: "notifications"
    depends_on:
      - database
    links:
      - database
  fake-twitch-websub:
    build: fake-twitch-websub
    ports:
      - "3001:3001"
  database:
    build: database
